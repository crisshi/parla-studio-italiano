const css=document.createElement('link');css.rel='stylesheet';css.href='lesson.css';document.head.append(css);document.querySelectorAll('.speak').forEach(b=>b.addEventListener('click',()=>{const u=new SpeechSynthesisUtterance(b.dataset.word);u.lang='en-GB';speechSynthesis.cancel();speechSynthesis.speak(u)}));document.querySelector('#read').addEventListener('click',()=>{const u=new SpeechSynthesisUtterance([...document.querySelectorAll('.reading p')].map(p=>p.textContent).join(' '));u.lang='en-GB';u.rate=.85;speechSynthesis.cancel();speechSynthesis.speak(u)});
